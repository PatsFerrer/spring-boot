#Spring Boot server configuration
# spring.datasource.url= jdbc:postgresql://localhost:5433/spring-boot-jdev
# spring.datasource.username=postgres
# spring.datasource.password=admin
# spring.jpa.hibernate.ddl-auto=update

#Corrige o erro que tava dando de 'M�todo org.postgresql.jdbc.PgConnection.createClob() ainda n�o foi implementado'

# spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults = false
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQL9Dialect

#nome de parte do caminho (chamado de contexto) que vai aparecer no site, ex: localhost:8000/springboot-rest-api/endpoint; 
# server.servlet.context-path=/springboot-rest-api

#server.address=0.0.0.0
#Se quiser trocar a porta do servidor:
#server.port=8000
# URL: http://localhost:8000/springboot-rest-api/

#H2 console web access configuration
#Open "http://0.0.0.0:8000/h2-console" and hit "Connect" button
# spring.h2.console.enabled=true
# spring.h2.console.path=/h2-console


# Pro Deploy no Render
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.datasource.username=${DATABASE_USERNAME}
spring.datasource.password=${DATABASE_PASSWORD}
spring.datasource.url=${DATABASE_URL}
spring.jpa.generate-ddl=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true